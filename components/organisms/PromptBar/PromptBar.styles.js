import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  container: {
    paddingVertical: 8,
    paddingLeft: 12,
    alignItems: "center",
  },
  scrollView: {
    maxHeight: 56,
  },
});
