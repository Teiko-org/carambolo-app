import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
    cardContainer: {
        display: 'flex',
    },
    infoContainer: {
        // display: 'flex',
        // flexDirection:
    }
})

export default styles
